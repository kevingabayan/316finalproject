const initState = {
    wireFramers: []
};

const wireFramerReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
            break;
    }
};

export default wireFramerReducer;